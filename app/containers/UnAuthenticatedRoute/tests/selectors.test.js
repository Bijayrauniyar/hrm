// import { selectUnAuthenticatedRouteDomain } from '../selectors';

describe('selectUnAuthenticatedRouteDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
